// eslint-disable-next-line no-used-vars
export interface MyObjectSchema {
    Registry: number;
    Name: string;
    OfficialName: string;
    Technician: string;
    Email: string;
    Phone: number;
}

// eslint-disable-next-line no-used-vars
export interface PTENG {
    PT: string;
    ENG: string;
}